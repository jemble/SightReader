<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@drawable/gradient_background"
    tools:context=".MidiPlayerActivity" >

     <TextView 
         android:id="@+id/midi_text_name"
         android:layout_width="match_parent"
         android:layout_height="wrap_content"
         android:layout_alignParentTop="true"/>
     
     <SeekBar
        android:id="@+id/midi_seek_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/midi_text_name"
        android:layout_alignParentLeft="true"
        android:layout_marginBottom="30dp" />
     
     <ImageButton 
        android:id="@+id/midi_btn_play"
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        android:layout_below="@+id/midi_seek_bar"
        android:layout_alignParentRight="true"
        android:onClick="btnPlay"
        android:background="@null"
        android:src="@drawable/button_play_layout"/>
     <ImageButton 
        android:id="@+id/midi_btn_stop"
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        android:layout_below="@+id/midi_seek_bar"
        android:layout_alignParentLeft="true"
        android:onClick="btnStop"
        android:background="@null"
        android:src="@drawable/button_stop_layout"/>
   

</RelativeLayout>
